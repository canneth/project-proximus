
import numpy as np

if __name__ == "__main__":
    num = 0.12153176963329315
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    num = np.sqrt(num)
    num = pow(num, 2)
    print(num)